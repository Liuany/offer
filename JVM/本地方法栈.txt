【属性】：线程私有，允许固定或者动态拓展内存大小，内存溢出方面与java虚拟机栈是一样的，用C语言实现，使用native关键字

【特性】
  ①当某个线程调用一个本地方法时，它就进入了一个全新的不受虚拟机限制的的世界，它与虚拟机拥有相同的权限。
    1> 本地方法可以通过本地方法接口访问虚拟机内部运行时的数据区。
    2> 可以使用本地处理器中的处理器。
    3> 直接从本地内存的堆中分配任意数量的内存。
  ②并不是所有的虚拟机都支持本地方法。
  ③HotSpot将本地方法栈和虚拟机合二为一了。